This was generated by converting the output of "./gactar -ebnf" using https://bottlecaps.de/convert/

That site will also generate nice railroad diagrams for the grammar.
---

AmodFile ::= sectiondelim 'model' sectiondelim ModelSection sectiondelim 'config' sectiondelim ConfigSection? sectiondelim 'init' sectiondelim InitSection? sectiondelim 'productions' sectiondelim ProductionSection?

ModelSection
         ::= 'name' ':' ( string | ident ) ( 'description' ':' string )? ( 'authors' '{' string* '}' )? ( 'examples' '{' Pattern* '}' )?

Pattern  ::= '[' ident ':' PatternSlot+ ']'

PatternSlot
         ::= '!'? ( 'nil' | ident | number | patternvar )
           | patternwildcard

ConfigSection
         ::= ( 'gactar' '{' Field* '}' )? ( 'modules' '{' Module* '}' )? ( 'chunks' '{' ChunkDecl* '}' )?

Field    ::= ident ':' FieldValue ','?

FieldValue
         ::= ident
           | string
           | number

Module   ::= ident '{' Field* '}'

ChunkDecl
         ::= '[' ident ':' ident+ ']'

InitSection
         ::= Initialization*

Initialization
         ::= ident ( '{' Pattern+ '}' | Pattern )

ProductionSection
         ::= Production+

Production
         ::= ident '{' ( 'description' ':' string )? Match Do '}'

Match    ::= 'match' '{' MatchItem+ '}'

MatchItem
         ::= ident Pattern WhenClause?

WhenClause
         ::= 'when' WhenExpression ( 'and' WhenExpression )*

WhenExpression
         ::= '(' patternvar ComparisonOperator Arg ')'

ComparisonOperator
         ::= equality
           | inequality

Arg      ::= patternvar
           | ident
           | string
           | number

Do       ::= 'do' '{' Statement+ '}'

Statement
         ::= ClearStatement
           | PrintStatement
           | RecallStatement
           | SetStatement
           | 'stop'

ClearStatement
         ::= 'clear' ( ident ','? )+

PrintStatement
         ::= 'print' ( Arg ','? )*

RecallStatement
         ::= 'recall' Pattern

SetStatement
         ::= 'set' ident ( '.' ident )? 'to' ( SetValue | Pattern )

SetValue ::= 'nil'
           | patternvar
           | string
           | number