linters:
  enable:
    - bodyclose
    - dupl
    - exportloopref
    - goconst
    - gocritic
    - goerr113
    - goprintffuncname
    - gosec
    - maintidx
    - prealloc
    - thelper
    - unconvert
    - unparam

linters-settings:
  govet:
    check-shadowing: true
  goconst:
    min-len: 8
    min-occurrences: 3
